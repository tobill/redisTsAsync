{
    "name": "redis-ts-async",
    "version": "0.1.3",
    "description": "redis lib for typescript with promises fopr async / await use",
    "repository": "",
    "author": "Tobias Billep <t.billep@googlemail.com>",
    "license": "MIT",
    "scripts": {
        "prepare": "npm run clean && npm run build",
        "build": "tsc.cmd",
        "clean": "rimraf rimraf dist es lib coverage"
    },
    "main": "lib/redisTsAsync.js",
    "dependencies": {
        "bluebird": "^3.5.0",
        "redis": "^2.7.1"
    },
    "devDependencies": {
        "@types/bluebird": "^3.5.8",
        "@types/redis": "^2.6.0",
        "typescript": "^2.4.1"
    }
}
